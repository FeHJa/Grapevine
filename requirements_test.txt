pytest>=8.0

# discovery.py has no Home Assistant dependency by design, so
# tests/test_discovery.py runs with pytest alone.
#
# config_flow.py, scheduler.py, adapters/legacy_discovery.py and
# __init__.py import homeassistant directly and are not exercised by any
# test in this repo yet — Phase 1's acceptance criteria for those modules
# (MIGRATION_PLAN.md) require pytest-homeassistant-custom-component plus
# a homeassistant install matching the 2026.7 core floor, which the
# sandbox this was authored in cannot provide. Add both once a matching
# dev environment is available:
#   pytest-homeassistant-custom-component
#   homeassistant>=2026.7
